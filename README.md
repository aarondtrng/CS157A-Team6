# SpartanFinds

A lost and found app for students at SJSU

## Dependencies

- [Node.js](https://nodejs.org/) v20+
- npm v10+

The frontend is built with React 19 and Vite. All other dependencies are installed via npm (see `Frontend/package.json`).

## Running the app

```bash
cd Frontend
npm install
npm run dev
```
